export default function Button({ children, variant = "primary", ...props}){ const base = "inline-flex items-center gap-2 rounded-xl px-4 py-2 text-sm font-medium transition"; const styles = variant === "ghost" ? "border border-amber-400/50 text-amber-300 hover:bg-amber-400 hover:text-slate-900" : "bg-amber-300 text-slate-900 hover:bg-amber-200"; return <button className={`${base} ${styles}`} {...props}>{children}</button> }
